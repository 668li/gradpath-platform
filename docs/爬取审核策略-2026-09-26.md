# 爬取审核策略 — 2026-09-26（B0② 交付物）

> 背景：marathon 任务清单 B0②——"疏通存储闸：official_announce 实测两次『抓 46 存 0』
> （去重+审核闸吃光）、t_review_queue_item 堆 5576 无人审；给官方域名+_fetch_log 留痕的线
> 启用快速通道（信任锚三条件已在代码）**或定明确审核策略**，否则爬了也存不进。"
> 本文件走第二选项：**定明确审核策略**，不动人审闸代码。

## 1. 为什么不走"自动 APPROVED 快速通道"

信任锚改革（09-06 拍板）的核心不变量：**未审核内容不面客**。入库侧自动 APPROVED 会
重新打开"爬虫直写面客"的口子（bilibili 106 条事故的死因之一就是未验证内容面客）。
官方域名只证明"内容来自官方页面"，不证明"解析正确、无截断、无串页"——46 抓 0 存的
教训恰是解析产物不可全信。

## 2. 复喂范围（B0③，2026-09-26 生效）

| 线 | schedule | 产出去向 | 状态 |
|---|---|---|---|
| official_announce | 每小时整点 | ext_research（PENDING） | ✅ 复喂 |
| eol_kaoyan | 每日 02:00 | ext_research（PENDING） | ✅ 复喂 |
| rsshub_research | 每日 02:30 | ext_research（PENDING） | ✅ 复喂 |
| web_article_research / news_aggregates | 手动 | — | 保持休眠（无重启拍板） |
| yanzhao / yanzhao_program / real_data | 手动 | — | 维持现状 |

## 3. 审核策略（PENDING 走查 SLA）

1. **走查节奏**：每周一次批量走查 `t_review_queue_item` 中 `item_type='external_research'`
   的 PENDING 条目（approve/reject 有据，参照 09-26 B5 走查口径：招生情报=APPROVE、
   校内行政通知=REJECT）。
2. **积压告警线**：PENDING > 200 时视为堆积（历史事故水位），在周报/运维检查中必须处置。
3. **冻结窗**：冻结窗内暂停走查外的线变更；窗内 PENDING 允许自然堆积，窗后一次性走查。
4. **预审标记（后续可选）**：若堆积常态化，再评估给官方域名+_fetch_log 留痕条目加
   "预审建议"标记辅助人工提速——属代码改动，届时另行立案。

## 4. 配套基建修复（同批上产）

- **yaml 进镜像**：pyproject `[tool.setuptools.package-data]` 补 `config/*.yaml`——此前
  wheel 不含数据文件，生产 `load_lines()` 空注册 → `default_schedules()` 空 → 全线静默。
- **seed 只增不删修复**：`seed_default_schedules()` 启动时清理默认表外的残留
  `crawler_*` job——停线后 Redis jobstore 残留 job 不再复活（09-19 停喂须 hdel 实锤）。
- **回调查 enabled 修复**：`_run_scheduled_crawler` 运行时查线注册与 enabled——
  停用线即时生效，不依赖重启清理。
