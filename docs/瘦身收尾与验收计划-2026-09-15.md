# 瘦身收尾与验收计划（2026-09-15）

> 来源：09-14 用户回贴 A 类计划并「开工」→ A1–A7 已执行并两次上产；09-15 用户拍板「删了（暗知识生产数据）」+「继续解决发现的问题」。
> 本文只列**尚未完成**的事，已完成部分见每节「已闭环」行与 `docs/承诺台账.md`。

## 0. 已闭环（本轮，不再重复劳动）

- A1–A7 全部上产，两次发射（服务器 HEAD `20f20eb` → `1fc45dd`），8 容器 healthy，alembic 未变；生产冒烟=8 条删除路由 404 / 4 条保留路由 200 / 假 skill 0 / admin 403 / 首页四身份且零「暗知识」。
- 生产暗知识数据已清（用户拍板）：`dark_knowledge` 37 行 + `career_dark_knowledge` 20 行 + `document_embeddings` 37 条向量，事务提交后逐表归零、向量总数 1925→1888 精确吻合；备份在服务器 `/tmp/bak_dark_knowledge_20260914.sql` 与本地 `legacy/`。
- 红线缺口已拆：`career-intel /dark-knowledge/stages` 读时自动播种、grad/career 硬编码种子、`research_promote` 唯一落表通道；爬虫 item_type 纠正为 `external_research`。
- 残留清理已提交（`62d5d7b`）：war-room 两套虚拟列表（−382 行）、search 类型筛选与文案、exam-timeline 角标、kaoyan-compare 洞察块、死类型全清；README/DOCKER_README/n8n SETUP/对外材料同步（去「10 万暗知识」数字主张）。

## 1. P1 需要你一句话拍板的两件（都是不可逆或触碰他人地界）

1. **三张空表的 drop 迁移**（`dark_knowledge` / `career_dark_knowledge` / `civil_service_dark_knowledge`，另 `dark_knowledge_push_log`）与 `mentors` / `mentor_reviews`。
   - 现状：表已 0 行，模型/服务/端点全删，属纯死表。
   - 风险点：**部署管道无条件跑 `alembic upgrade head`**，所以"写迁移"等于"下次部署即执行 drop"——不是可以延后再定的动作。drop 会丢表内任何历史行（当前 0 行）与表级索引。
   - 我的建议：现在做（趁已备份、趁没有并发写），但需要你明确「同意 drop」四个字。
2. **服务器工作树 `nginx/nginx.conf` 入库**（线上 TLS/安全头配置，+70 行，只在服务器、git 里没有）。
   - 现状：只读留档 `legacy/nginx.conf.srv-live-20260914`；每次部署前须核它不被 bundle 触碰。
   - 建议：由**并行会话**入库（那是他们的在途改动），我入库会与他们撞车。若你说"你入"，我就入并同步台账。

## 2. P2 真实浏览器验收（我可以直接做，不需要拍板）

本轮三个交互修复都是"点下去有没有反应"类，代码层已证（tsc/SSR/API 403），但**没在真浏览器点过**。计划在生产 `https://quxianglab.cn` 用浏览器实测四条：

| 验收项 | 判据 |
|---|---|
| 导航分组父条目 | 点「决策 · 做出选择」→ 进入 `/decision-center`；点右侧箭头 → 只展开/收起 |
| 技能树页 | 打开不再出现"能力地图加载失败"；技能树与"去模拟"入口可见 |
| 移动端（375px） | 顶栏命令面板图标**一次点击**打开面板；抽屉底部「命令面板」不再被截断 |
| `/admin` | 非管理员账号点入 → 被重定向到 `/dashboard`（补上 A8 唯一没做的浏览器那一步） |

配套：需要一个非管理员冒烟账号（建→验→删，走 09-14 已实证的 psql 清理路径）。

## 3. P3 B 类剩余（你已拍 B1/B2/B3，剩四项待拍 + 一项待料）

- **B4 路径模拟器**：收敛为"三线各一条主路径"够不够？还是连模拟器都先砍只留时间线？
- **B5 AI 对话 skill 收敛 10→4**：确认留「流程伴随官 / 公告解读 / 人生设计 / 通用咨询（明示走搜索）」；聊天里内推助手/薪资基准/公司评价/简历优化是否一并下架。
- **B6 专业前景做结论层**（6 档出身个性化 + 条件账本联动），不做聚合搜索入口——是否排期。
- **B7 人生设计接行动层 + 测评溶解进对话**（ask-matt 4 修顺手清）。
- **B8 复盘 skill**：等你给文件，挂起待料不阻塞。

## 4. P4 已排定的时间线（提醒，不是待办）

- **09-18**：行为设计观察窗到点，按**面板口径**重读续做率→决定 90 天冲刺/学习计划的折叠动作。
- **09-22**：D1–D3（Q-A/B1/B2）拍板死线，否则 Phase2 顺延错过公告日。
- **09-23–10-02**：山东省考窗口（004 线就绪）。
- **10-15**：K1 简章季三选一禁拖。

## 5. P5 欠账提醒（本轮反复被提示、仍未补）

- **Mimosa 完整审计（scanner_enobufs）**：本轮每次提交都回"未得到完整扫描结论（兼容策略继续）"。按纪律**不得宣称项目安全**。建议单独跑一轮完整审计并销账。
- 台账未销账 5 条见 `docs/承诺台账.md`（暗知识四表处置已有结论待执行 drop、mentors drop、nginx.conf 入库、残留面、对外材料——后两条本轮已部分闭环，销账时会更新）。

## 6. 执行顺序建议

P2（真浏览器验收，可立刻做）→ P1-1（drop 迁移，等你四字）→ P1-2（nginx 入库，倾向交给并行会话）→ P3（B 类逐项拍板后开工）→ P5（审计补跑）。

每步仍守既有纪律：显式路径 add、commit 前核 `git diff --cached`、禁 `reset --hard`、部署走 gradpath-deploy 四闸 + `git show --stat` 验产物、冒烟绑本次特有断言、删数据先备份、生产写操作先拍板。
