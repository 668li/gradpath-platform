# P2 功能合并大手术 · 任务书（/goal 可直接粘贴）

> 2026-09-06 ｜ 领导默认拍板：全清单执行（可裁剪）；旧路由一律 302 不留 404；只折叠入口不删组件。
> 交付形态：下面代码块整段粘贴到目标模式执行。P0+P1 已由管理者于 6b9d070 实施并部署。

```
你是执行者，本文档是唯一任务来源；中途没人可问，拿不准的写 BLOCKED.md（待裁决清单）跳过继续做别的，最后随交付提交。断了、换新会话：先读 PROGRESS.md 接着做，别重做；每做完一项立刻更新。
这活为什么干：用户拍板"多功能必须合在一起"——95 个路由只增不删把导航压垮；干完后菜单每组只有一个主入口，被折叠功能的老链接全部 302 到新家，零数据丢失。
让步顺序：不丢数据 > 链接不 404 > 改得全 > 改得快。
「只允许/不许」违反即失败；「建议」有更好的路可走，在 PROGRESS.md 记一句为什么。

【我替领导拍的板】
- 被折叠功能的旧路由一律在 next.config redirects 里 302 到新家（猜的；猜错代价=收藏/外链断头）。
- 决策四页/计划页的组件代码全保留，只折叠入口+重定向，不删组件（猜的；保守可回滚）。
- /plans 并入 /study-plans；成长洞察(84行)并入成就墙页尾；/insights 302 到 /achievements。
- 菜单组名与组内入口：决策组只留 /decision-center；执行组留 /actions + /study-plans + /timeline；定位组删 /life-wheel；成长组只留"成长回顾"→/achievements。

【界限】
- 只允许改：frontend/components/nav.tsx、frontend/lib/commands.ts、frontend/next.config.*(redirects)、被折叠页的 page.tsx（改薄壳/redirect）、frontend/app/(app)/achievements/page.tsx。其余只读。
- 判卷标准（tests/ 目录、*.test.ts）碰都不许碰。
- 顺手活禁止：不重构样式、不删"看着没用"的其他页面、不动 backend/——想动写 BLOCKED.md。

【现状与任务0】（2026-09-06 实测，生产 HEAD=6b9d070 含 P0 修复）
- 仓库 D:\职业规划\职业规划，改动在 frontend/；菜单树=nav.tsx 的 getNavSections()；命令盘条目=lib/commands.ts（现 70 条，含 admin 标记）。
- 任务0：cd frontend && npm run lint（warning 级可存在，不得新增 error）→ npx vitest run（基线 183 全绿 skipped=0）→ npm run build 成功。对不上就停：证据写 BLOCKED.md 最上面，只做不受影响的部分；核对无误后把「理解的目标/顺序/最大风险」≤10 行写进 PROGRESS.md 再动工。

【任务1 菜单与命令盘收敛】
nav.tsx：决策组 children 只留 {/decision-center 决策中心}；成长组 children 只留 {/achievements 成长回顾}；定位组 children 删 life-wheel；执行组 children 只留 actions/study-plans/timeline。commands.ts 删除 career、life-wheel、insights、decision-lab、decision-engine、war-room、decisions、plans 八个条目。
验收：grep -rn "life-wheel\|decision-lab\|decision-engine\|war-room" frontend/components/nav.tsx frontend/lib/commands.ts 输出为空；npm run build 绿。

【任务2 旧路由 302】
next.config redirects 追加：/career→/dashboard、/life-wheel→/self-discovery、/insights→/achievements、/decision-lab→/decision-center、/decision-engine→/decision-center、/war-room→/decision-center、/decisions→/decision-center、/plans→/study-plans。
验收：grep -c "destination" frontend/next.config.* ≥8 且八条路径逐一在配置里 grep 得到。

【任务3 成就墙吸收成长洞察】
把 app/(app)/insights/page.tsx 的数据展示区块整体搬入 achievements/page.tsx 页尾（insights 的 page.tsx 改为空壳并依赖任务2 的 302）。
验收：npx vitest run 通过数 ≥183 且 skipped=0；grep "insights" frontend/app/(app)/achievements/page.tsx 非空。

【任务4 收尾自查】
npm run lint → npx vitest run → npm run build 三绿。
禁止（全算失败）：skip/删测试、放宽断言、mock 被测对象、改验收脚本、|| true。测试数只许 ≥183。
同一条验收连败 3 次换下一项；结果比开工差就回滚如实报告——"没做成但说清了"合格，"做了但更糟"不合格。

【完成条件】
- 硬指标1：任务1/2 的 grep 判定逐条贴出实际输出（全 0 / ≥8）。
- 硬指标2：vitest ≥183 且 skipped=0、build 成功，贴输出。
- 每条验收都要在对话里贴实际命令输出（含失败→修复→变绿证据），只说做完了不算。
- BLOCKED.md 随交付提交，空的也写「无」。
- 止损：跑满 3 轮即停，如实汇报卡在哪、还差什么。
```

## 暗卷（管理者自留，执行者不可见）
1. 部署后 `curl -s http://82.156.236.152/life-wheel -o /dev/null -w "%{http_code}"` 须 30x 落 /self-discovery。
2. 生产容器 .next 产物里 `grep -rl "人生平衡轮" .next/server` 应只在 redirect 配置残余，不再有独立页面壳。
3. 手机 UA 走查：菜单"决策"组只展开一项；命令盘搜"爬虫"对普通账号无结果（P0 暗卷同源）。
